#include "urav.h"
#include "ctest.h"

CTEST(urav_suite, test__equation) 
{
	const int a =- 1, b = 3, c = 3;

        float x1, x2;

 	urav (a, b, c, &x1, &x2);
 
	const float expected_x1 = 0;
	const float expected_x2 = 3;
	
	ASSERT_DBL_NEAR(expected_x1, x1);
	ASSERT_DBL_NEAR(expected_x2, x2);
}


